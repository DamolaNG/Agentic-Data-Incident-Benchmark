version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: orders
        description: Synthetic order source data loaded into DuckDB.
        columns:
          - name: order_id
            tests:
              - not_null
              - unique
          - name: customer_id
            tests:
              - not_null
          - name: order_timestamp
            tests:
              - not_null
          - name: order_status
            tests:
              - not_null
          - name: order_total
            tests:
              - not_null

