# Data

This folder contains local data used by the benchmark.

- `generated/` contains synthetic source files created by the data generation step.
- `raw/` is reserved for raw files copied or staged before ingestion.

Generated files are ignored by Git so local test data does not pollute commits.

