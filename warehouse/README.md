# Warehouse

DuckDB is the local analytical warehouse for this benchmark.

The default database path is:

```text
warehouse/local/benchmark.duckdb
```

Local database files are ignored by Git because they are generated artifacts.

